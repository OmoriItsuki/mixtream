using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Debug.Log("アーケア方式:"+(1000000*ComboManager.combo/GameManager.Notes.Count+ComboManager.maxCombo*ComboManager.maxCombo/GameManager.Notes.Count/2)+
          //        "\nCytus方式:"+(900000*ComboManager.combo/GameManager.Notes.Count+100000*ComboManager.maxCombo/GameManager.Notes.Count));
    }
}
